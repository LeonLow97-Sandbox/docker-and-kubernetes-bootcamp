apiVersion: v1
kind: Pod
metadata:
  name: myapp-pod
spec:
  containers:
  - name: data-processor
    image: data-processor
  nodeSelector:
    size: Large   # key-value pair matches `labels` on node, label the nodes prior to creating the pod

# kubectl label nodes node-1 size=Large
