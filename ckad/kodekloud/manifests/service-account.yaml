apiVersion: v1
kind: Pod
metadata:
  name: my-kubernetes-dashboard
spec:
  containers:
    - name: my-kubernetes-dashboard
      image: nginx
  serviceAccountName: dashboard-sa
  # automountServiceAccountToken: false # if you do not want to mount serviceAccount automatically
